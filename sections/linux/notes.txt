[vagrant@vbox ~]$ cd /tmp

[vagrant@vbox tmp]$
[vagrant@vbox tmp]$ cp /home/vagrant/devopsfile2.txt /home/vagrant/dev
[vagrant@vbox tmp]$ ls
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-chronyd.service-LXYFvx
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-dbus-broker.service-Y4QCHG
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-irqbalance.service-nlsNTs
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-kdump.service-Wz12Kh
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-systemd-logind.service-xQ1WJu
[vagrant@vbox tmp]$ cd ~
[vagrant@vbox ~]$ pwd
/home/vagrant
[vagrant@vbox ~]$ cp -r dev backupdir
[vagrant@vbox ~]$ ls
backupdir         devopsfile2.txt  devopsfile6.txt  ops
dev               devopsfile3.txt  devopsfile7.txt  testfile1.txt
devopsfile10.txt  devopsfile4.txt  devopsfile8.txt
devopsfile1.txt   devopsfile5.txt  devopsfile9.txt
[vagrant@vbox ~]$ ls backupdir
dev
[vagrant@vbox ~]$ ls dev
devopsfile1.txt  devopsfile2.txt
[vagrant@vbox ~]$ cp --help
Usage: cp [OPTION]... [-T] SOURCE DEST
  or:  cp [OPTION]... SOURCE... DIRECTORY
  or:  cp [OPTION]... -t DIRECTORY SOURCE...
Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY.

Mandatory arguments to long options are mandatory for short options too.
  -a, --archive                same as -dR --preserve=all
      --attributes-only        don't copy the file data, just the attributes
      --backup[=CONTROL]       make a backup of each existing destination file
  -b                           like --backup but does not accept an argument
      --copy-contents          copy contents of special files when recursive
  -d                           same as --no-dereference --preserve=links
  -f, --force                  if an existing destination file cannot be
                                 opened, remove it and try again (this option
                                 is ignored when the -n option is also used)
  -i, --interactive            prompt before overwrite (overrides a previous -n
                                  option)
  -H                           follow command-line symbolic links in SOURCE
  -l, --link                   hard link files instead of copying
  -L, --dereference            always follow symbolic links in SOURCE
  -n, --no-clobber             do not overwrite an existing file (overrides
                                 a previous -i option)
  -P, --no-dereference         never follow symbolic links in SOURCE
  -p                           same as --preserve=mode,ownership,timestamps
      --preserve[=ATTR_LIST]   preserve the specified attributes (default:
                                 mode,ownership,timestamps), if possible
                                 additional attributes: context, links, xattr,
                                 all
  -c                           deprecated, same as --preserve=context
      --no-preserve=ATTR_LIST  don't preserve the specified attributes
      --parents                use full source file name under DIRECTORY
  -R, -r, --recursive          copy directories recursively
      --reflink[=WHEN]         control clone/CoW copies. See below
      --remove-destination     remove each existing destination file before
                                 attempting to open it (contrast with --force)
      --sparse=WHEN            control creation of sparse files. See below
      --strip-trailing-slashes  remove any trailing slashes from each SOURCE
                                 argument
  -s, --symbolic-link          make symbolic links instead of copying
  -S, --suffix=SUFFIX          override the usual backup suffix
  -t, --target-directory=DIRECTORY  copy all SOURCE arguments into DIRECTORY
  -T, --no-target-directory    treat DEST as a normal file
  -u, --update                 copy only when the SOURCE file is newer
                                 than the destination file or when the
                                 destination file is missing
  -v, --verbose                explain what is being done
  -x, --one-file-system        stay on this file system
  -Z                           set SELinux security context of destination
                                 file to default type
      --context[=CTX]          like -Z, or if CTX is specified then set the
                                 SELinux or SMACK security context to CTX
      --help     display this help and exit
      --version  output version information and exit

By default, sparse SOURCE files are detected by a crude heuristic and the
corresponding DEST file is made sparse as well.  That is the behavior
selected by --sparse=auto.  Specify --sparse=always to create a sparse DEST
file whenever the SOURCE file contains a long enough sequence of zero bytes.
Use --sparse=never to inhibit creation of sparse files.

When --reflink[=always] is specified, perform a lightweight copy, where the
data blocks are copied only when modified.  If this is not possible the copy
fails, or if --reflink=auto is specified, fall back to a standard copy.
Use --reflink=never to ensure a standard copy is performed.

The backup suffix is '~', unless set with --suffix or SIMPLE_BACKUP_SUFFIX.
The version control method may be selected via the --backup option or through
the VERSION_CONTROL environment variable.  Here are the values:

  none, off       never make backups (even if --backup is given)
  numbered, t     make numbered backups
  existing, nil   numbered if numbered backups exist, simple otherwise
  simple, never   always make simple backups

As a special case, cp makes a backup of SOURCE when the force and backup
options are given and SOURCE and DEST are the same name for an existing,
regular file.

GNU coreutils online help: <https://www.gnu.org/software/coreutils/>
Full documentation <https://www.gnu.org/software/coreutils/cp>
or available locally via: info '(coreutils) cp invocation'
[vagrant@vbox ~]$ pwd
/home/vagrant
[vagrant@vbox ~]$ ls
backupdir         devopsfile2.txt  devopsfile6.txt  ops
dev               devopsfile3.txt  devopsfile7.txt  testfile1.txt
devopsfile10.txt  devopsfile4.txt  devopsfile8.txt
devopsfile1.txt   devopsfile5.txt  devopsfile9.txt
[vagrant@vbox ~]$ mv devopsfile4.txt ops
[vagrant@vbox ~]$ ls ops
devopsfile4.txt
[vagrant@vbox ~]$ mkdir textdir
[vagrant@vbox ~]$ mv *.txt textdir
[vagrant@vbox ~]$ ls textdir
devopsfile10.txt  devopsfile3.txt  devopsfile7.txt  testfile1.txt
devopsfile1.txt   devopsfile5.txt  devopsfile8.txt
devopsfile2.txt   devopsfile6.txt  devopsfile9.txt
[vagrant@vbox ~]$ ls
backupdir  dev  ops  textdir
[vagrant@vbox ~]$ rm dev
rm: cannot remove 'dev': Is a directory
[vagrant@vbox ~]$ rm -r dev
[vagrant@vbox ~]$ rm textdir/devopsfile1.txt
[vagrant@vbox ~]$ ls textdir
devopsfile10.txt  devopsfile5.txt  devopsfile8.txt
devopsfile2.txt   devopsfile6.txt  devopsfile9.txt
devopsfile3.txt   devopsfile7.txt  testfile1.txt
[vagrant@vbox ~]$ cd textdir
[vagrant@vbox textdir]$ ls
devopsfile10.txt  devopsfile5.txt  devopsfile8.txt
devopsfile2.txt   devopsfile6.txt  devopsfile9.txt
devopsfile3.txt   devopsfile7.txt  testfile1.txt
[vagrant@vbox textdir]$ rm -rf *
[vagrant@vbox textdir]$ ls
[vagrant@vbox textdir]$ /home
-bash: /home: Is a directory
[vagrant@vbox textdir]$ cd /home
[vagrant@vbox home]$ /home
-bash: /home: Is a directory
[vagrant@vbox home]$ cd ~
[vagrant@vbox ~]$ history
    1  clear
    2  whoami
    3  pwd
    4  sudo -i
    5  exit
    6  /root
    7  ls
    8  cd /
    9  ls
   10  cat /etc/os-release
   11  cd /etc
   12  ls
   13  cat grub2.cfg
   14  cat issue
   15  whoami
   16  sudo -i
   17  whoami
   18  ls
   19  cd /
   20  cd ..
   21  cd /root
   22  /root
   23  cd /
[vagrant@vbox ~]$ cd /tmp
[vagrant@vbox tmp]$
[vagrant@vbox tmp]$ cp /home/vagrant/devopsfile2.txt /home/vagrant/dev
[vagrant@vbox tmp]$ ls
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-chronyd.service-LXYFvx
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-dbus-broker.service-Y4QCHG
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-irqbalance.service-nlsNTs
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-kdump.service-Wz12Kh
systemd-private-c477b2d8e9f24da8bbca1c06eb08fbba-systemd-logind.service-xQ1WJu
[vagrant@vbox tmp]$ cd ~
[vagrant@vbox ~]$ pwd
/home/vagrant
[vagrant@vbox ~]$ cp -r dev backupdir
[vagrant@vbox ~]$ ls
backupdir         devopsfile2.txt  devopsfile6.txt  ops
dev               devopsfile3.txt  devopsfile7.txt  testfile1.txt
devopsfile10.txt  devopsfile4.txt  devopsfile8.txt
devopsfile1.txt   devopsfile5.txt  devopsfile9.txt
[vagrant@vbox ~]$ ls backupdir
dev
[vagrant@vbox ~]$ ls dev
devopsfile1.txt  devopsfile2.txt
[vagrant@vbox ~]$ cp --help
Usage: cp [OPTION]... [-T] SOURCE DEST
  or:  cp [OPTION]... SOURCE... DIRECTORY
  or:  cp [OPTION]... -t DIRECTORY SOURCE...
Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY.

Mandatory arguments to long options are mandatory for short options too.
  -a, --archive                same as -dR --preserve=all
      --attributes-only        don't copy the file data, just the attributes
      --backup[=CONTROL]       make a backup of each existing destination file
  -b                           like --backup but does not accept an argument
      --copy-contents          copy contents of special files when recursive
  -d                           same as --no-dereference --preserve=links
  -f, --force                  if an existing destination file cannot be
                                 opened, remove it and try again (this option
                                 is ignored when the -n option is also used)
  -i, --interactive            prompt before overwrite (overrides a previous -n
                                  option)
  -H                           follow command-line symbolic links in SOURCE
  -l, --link                   hard link files instead of copying
  -L, --dereference            always follow symbolic links in SOURCE
  -n, --no-clobber             do not overwrite an existing file (overrides
                                 a previous -i option)
  -P, --no-dereference         never follow symbolic links in SOURCE
  -p                           same as --preserve=mode,ownership,timestamps
      --preserve[=ATTR_LIST]   preserve the specified attributes (default:
                                 mode,ownership,timestamps), if possible
                                 additional attributes: context, links, xattr,
                                 all
  -c                           deprecated, same as --preserve=context
      --no-preserve=ATTR_LIST  don't preserve the specified attributes
      --parents                use full source file name under DIRECTORY
  -R, -r, --recursive          copy directories recursively
      --reflink[=WHEN]         control clone/CoW copies. See below
      --remove-destination     remove each existing destination file before
                                 attempting to open it (contrast with --force)
      --sparse=WHEN            control creation of sparse files. See below
      --strip-trailing-slashes  remove any trailing slashes from each SOURCE
                                 argument
  -s, --symbolic-link          make symbolic links instead of copying
  -S, --suffix=SUFFIX          override the usual backup suffix
  -t, --target-directory=DIRECTORY  copy all SOURCE arguments into DIRECTORY
  -T, --no-target-directory    treat DEST as a normal file
  -u, --update                 copy only when the SOURCE file is newer
                                 than the destination file or when the
                                 destination file is missing
  -v, --verbose                explain what is being done
  -x, --one-file-system        stay on this file system
  -Z                           set SELinux security context of destination
                                 file to default type
      --context[=CTX]          like -Z, or if CTX is specified then set the
                                 SELinux or SMACK security context to CTX
      --help     display this help and exit
      --version  output version information and exit

By default, sparse SOURCE files are detected by a crude heuristic and the
corresponding DEST file is made sparse as well.  That is the behavior
selected by --sparse=auto.  Specify --sparse=always to create a sparse DEST
file whenever the SOURCE file contains a long enough sequence of zero bytes.
Use --sparse=never to inhibit creation of sparse files.

When --reflink[=always] is specified, perform a lightweight copy, where the
data blocks are copied only when modified.  If this is not possible the copy
fails, or if --reflink=auto is specified, fall back to a standard copy.
Use --reflink=never to ensure a standard copy is performed.

The backup suffix is '~', unless set with --suffix or SIMPLE_BACKUP_SUFFIX.
The version control method may be selected via the --backup option or through
the VERSION_CONTROL environment variable.  Here are the values:

  none, off       never make backups (even if --backup is given)
  numbered, t     make numbered backups
  existing, nil   numbered if numbered backups exist, simple otherwise
  simple, never   always make simple backups

As a special case, cp makes a backup of SOURCE when the force and backup
options are given and SOURCE and DEST are the same name for an existing,
regular file.

GNU coreutils online help: <https://www.gnu.org/software/coreutils/>
Full documentation <https://www.gnu.org/software/coreutils/cp>
or available locally via: info '(coreutils) cp invocation'
[vagrant@vbox ~]$ pwd
/home/vagrant
[vagrant@vbox ~]$ ls
backupdir         devopsfile2.txt  devopsfile6.txt  ops
dev               devopsfile3.txt  devopsfile7.txt  testfile1.txt
devopsfile10.txt  devopsfile4.txt  devopsfile8.txt
devopsfile1.txt   devopsfile5.txt  devopsfile9.txt
[vagrant@vbox ~]$ mv devopsfile4.txt ops
[vagrant@vbox ~]$ ls ops
devopsfile4.txt
[vagrant@vbox ~]$ mkdir textdir
[vagrant@vbox ~]$ mv *.txt textdir
[vagrant@vbox ~]$ ls textdir
devopsfile10.txt  devopsfile3.txt  devopsfile7.txt  testfile1.txt
devopsfile1.txt   devopsfile5.txt  devopsfile8.txt
devopsfile2.txt   devopsfile6.txt  devopsfile9.txt
[vagrant@vbox ~]$ ls
backupdir  dev  ops  textdir
[vagrant@vbox ~]$ rm dev
rm: cannot remove 'dev': Is a directory
[vagrant@vbox ~]$ rm -r dev
[vagrant@vbox ~]$ rm textdir/devopsfile1.txt
[vagrant@vbox ~]$ ls textdir
devopsfile10.txt  devopsfile5.txt  devopsfile8.txt
devopsfile2.txt   devopsfile6.txt  devopsfile9.txt
devopsfile3.txt   devopsfile7.txt  testfile1.txt
[vagrant@vbox ~]$ cd textdir
[vagrant@vbox textdir]$ ls
devopsfile10.txt  devopsfile5.txt  devopsfile8.txt
devopsfile2.txt   devopsfile6.txt  devopsfile9.txt
devopsfile3.txt   devopsfile7.txt  testfile1.txt
[vagrant@vbox textdir]$ rm -rf *
[vagrant@vbox textdir]$ ls
[vagrant@vbox textdir]$ /home
-bash: /home: Is a directory
[vagrant@vbox textdir]$ cd /home
[vagrant@vbox home]$ /home
-bash: /home: Is a directory
[vagrant@vbox home]$ cd ~
[vagrant@vbox ~]$ history
    1  clear
    2  whoami
    3  pwd
    4  sudo -i
    5  exit
    6  /root
    7  ls
    8  cd /
    9  ls
   10  cat /etc/os-release
   11  cd /etc
   12  ls
   13  cat grub2.cfg
   14  cat issue
   15  whoami
   16  sudo -i
   17  whoami
   18  ls
   19  cd /
   20  cd ..
   21  cd /root
   22  /root
   23  cd /
   24  exit
   25  ls
   26  pwd
   27  mkdir dev
   28  mkdir ops backupdir
   29  ls
   30  pwd
   31  touch testfile1.txt
   32  ls
   33   touch devopsfile{1..10}.txt
   34  ls
   35  cp devopsfile1.txt dev/
   36  ls dev/
   37  cd /tmp/
   38  ls /home/vagrant/dev
   39  cd
   40  clear
   41  cd /tmp
   42  cp /home/vagrant/devopsfile2.txt /home/vagrant/dev
   43  ls
   44  cd ~
   45  pwd
   46  cp -r dev backupdir
   47  ls
   48  ls backupdir
   49  ls dev
   50  cp --help
   51  pwd
   52  ls
   53  mv devopsfile4.txt ops
   54  ls ops
   55  mkdir textdir
   56  mv *.txt textdir
   57  ls textdir
   58  ls
   59  rm dev
   60  rm -r dev
   61  rm textdir/devopsfile1.txt
   62  ls textdir
   63  cd textdir
   64  ls
   65  rm -rf *
   66  ls
   67  /home
   68  cd /home
   69  /home
   70  cd ~
   71  history
[vagrant@vbox ~]$ exit
    1  clear
    2  whoami
    3  pwd
    4  sudo -i
    5  exit
    6  /root
    7  ls
    8  cd /
    9  ls
   10  cat /etc/os-release
   11  cd /etc
   12  ls
   13  cat grub2.cfg
   14  cat issue
   15  whoami
   16  sudo -i
   17  whoami
   18  ls
   19  cd /
   20  cd ..
   21  cd /root
   22  /root
   23  cd /
   24  exit
   25  ls
   26  pwd
   27  mkdir dev
   28  mkdir ops backupdir
   29  ls
   30  pwd
   31  touch testfile1.txt
   32  ls
   33   touch devopsfile{1..10}.txt
   34  ls
   35  cp devopsfile1.txt dev/
   36  ls dev/
   37  cd /tmp/
   38  ls /home/vagrant/dev
   39  cd
   40  clear
   41  cd /tmp
   42  cp /home/vagrant/devopsfile2.txt /home/vagrant/dev
   43  ls
   44  cd ~
   45  pwd
   46  cp -r dev backupdir
   47  ls
   48  ls backupdir
   49  ls dev
   50  cp --help
   51  pwd
   52  ls
   53  mv devopsfile4.txt ops
   54  ls ops
   55  mkdir textdir
   56  mv *.txt textdir
   57  ls textdir
   58  ls
   59  rm dev
   60  rm -r dev
   61  rm textdir/devopsfile1.txt
   62  ls textdir
   63  cd textdir
   64  ls
   65  rm -rf *
   66  ls
   67  /home
   68  cd /home
   69  /home
   70  cd ~
   71  history
   72  exit
   73  whoami
   74  cat etc/os-release
   75  cat /etc/os-release
   76  sudo yum install vim -y
   77  ls
   78  vim firstfile.txt
   79  cat firstfile.txt
   80  vim firstfile.txt
   81  cat firstfile.txt
   82  sudo -i
   83  logout
   84  hostname
   85  ls -ltr /etc
   86  history
[vagrant@centos ~]$ logout

    1  whoami
    2  exit
    3  pwd
    4  ls
    5  cat anacondo-ks.cfg
    6  cat /anaconda-ks.cfg
    7  cat anaconda-ks.cfg
    8  vim anaconda-ks.cfg
    9  whoami
   10  ls -l
   11  mkdir devopsdir
   12  ls
   13  ls -l
   14  file anaconda-ks.cfg
   15  /bin
   16  cd /bin
   17  ls
   18  ls -l
   19  file yum
   20  file zdump
   21  cd
   22  ls -l
   23  whoami
   24  cd /dev
   25  ls -l
   26  cd
   27  pwd
   28  ls
   29  mkdir /opt/dev/ops/devops/test
   30  mkdir -p /opt/dev/ops/devops/test
   31  ls
   32  vim /opt/dev/ops/devops/test/commands
   33  ln -s /opt/dev/ops/devops/test/commands
   34  cat /opt/dev/ops/devops/test/commands
   35  ln -s /opt/dev/ops/devops/test/commands cmd
   36  ls -l
   37  rm cmd
   38  ls
   39  ls -l
   40  rm cmd
   41  ls
   42  ls -l
   43  ls ltr /etc
   44  ls -ltr /etc
   45  ls /etc/hostname
   46  ls -l /etc/hostname
   47  vim /etc/hostname
   48  hostname centos7.devops.in
   49  logout
   50  history
    61  echo 'export HISTCONTROL=ignoreboth:erasedups' >> ~/.bashrc
   62  echo 'export HISTSIZE=10000' >> ~/.bashrc
   63  echo 'export HISTFILESIZE=20000' >> ~/.bashrc
   64  echo 'shopt -s histappend' >> ~/.bashrc
   65  echo 'PROMPT_COMMAND="history -a; history -n"' >> ~/.bashrc
   66  source ~/.bashrc
   67  history -n
   68  history -a
   69  history -w
   70  history -w
   71  historu
   72  history
   73  date
   74  clear
   75  history
   76  clear
   77  exit
   78  clear
   79  cd /var/log
   80  tail -f yum.log
   81  ls
   82  tail -f wtmp
   83  history
   84  ls
   85  less anaconda-ks.cfg
   86  more anaconda-ks.cfg
   87  less
   88  less anaconda-ks.cfg
   89  tail anaconda-ks.cfg
   90  head 3 anacondakes.cfg
   91  head 3 anaconda-ks.cfg
   92  tail 3 anaconda-ks.cfg
   93  tail -f anaconda-ks.cfg
   94  tail -f /var/log/messages
   95  cat etc/passwd
   96  cat /etc/passwd
   97  cut -d: -f -f1 /etc/passwd
   98  cut -d: -f2 /etc/passwd
   99  cut -d: -f1 /etc/passwd
  100  awk -F':' '{print $1}' /etc/passwd
  101  touch test.txt
  102  ls
  103  vim test.txt
  104  cat test.txt
  105  vim test.txt
  106  cat test.txt
  107  cat 's/coronavirus/covid19/g' *
  108  sed 's/coronavirus/covid19/g'
  109  history
  110  uptime
  111  uptime > /tmp/sysinfo.txt
  112  ls
  113  /tmp/sysinfo.txt
  114  cat /tmp/sysinfo.txt
  115  free -m
  116  df -h
  117  echo "Good Morning"
  118  echo "############################" > /tmp/sysinfo.txt
  119  date > /tmp/sysinfo.txt
  120  echo date > /tmp/sysinfo.txt
  121  echo "############################" >> /tmp/sysinfo.txt
  122  uptime >> /tmp/sysinfo.txt
  123  echo "############################" > /tmp/sysinfo.txt
  124  df -h >> /tmp/sysinfo.txt
  125  echo "############################" > /tmp/sysinfo.txt
  126  free -m >> /tmp/sysinfo.txt
  127  cat /tmp/sysinfo.txt
  128  date > /tmp/sysinfo.txt
  129  echo "############################" > /tmp/sysinfo.txt
  130  echo "############################" >> /tmp/sysinfo.txt
  131  date > /tmp/sysinfo.txt
  132  echo "############################" >> /tmp/sysinfo.txt
  133  uptime >> /tmp/sysinfo.txt
  134  echo "############################" >> /tmp/sysinfo.txt
  135  free -m >> /tmp/sysinfo.txt
  136  echo "############################" >> /tmp/sysinfo.txt
  137  df -h >> /tmp/sysinfo.txt
  138  echo "############################" >> /tmp/sysinfo.txt
  139  cat /tmp/sysinfo.txt
  140  cat /dev/null
  141  ls /dev/null
  142  cat /tmp/sysinfo.txt
  143  /tmp/sysinfo.txt > /dev/null
  144  cat /tmp/sysinfo.txt > /dev/null
  145  cat /tmp/sysinfo.txt
  146  cat /dev/null
  147  free -m >> /tmp/sysinfo.txt
  148  cat /tmp/sysinfo.txt
  149  freee -m >> /tmp/sysinfo.txt
  150  freee -m 2>> /tmp/sysinfo.txt
  151  cat /tmp/sysinfo.txt
  152  free -m &>> /tmp/sysinfo.txt
  153  cat /tmp/sysinfo.txt
  154  history
  155  clear
  156  wc -l /etc/passwd
  157  wc -l < /etc/passwd
  158  cd /etc/
  159  ls
  160  clear
  161  ls | wc -l
  162  ls | grep host
  163  tail /var/log/messages | grep -i vagrant
  164  tail -20 /var/log/messages | grep -i vagrant
  165  cat /var/log/messages
  166  tail -20 /var/log/messages | grep -i bound
  167  free -m | grep Mem
  168  clear
  169  cd
  170  whoami
  171  find /etc -name host*
  172  clear
  173  history
  174  clear
  175  history
  176  Mem
  177  grep mem
  178  whoami
  179  cat /etc/passwd
  180  head -1 /etc/passwd
  181  grep vagrant etc/passwd
  182  grep vagrant /etc/passwd
  183  cat /etc/passwd
  184  /etc/shadow
  185  sudo cat /etc/shadow
  186  grep vagrant /etc/passwd
  187  id vagrant
  188  useradd ansible
  189  useradd jenkins
  190  useradd aws
  191  tail -4 /etc/passwd
  192  id ansible
  193  group ansible
  194  groupadd devops
  195  clear
  196  usermod -aG devops ansible
  197  id ansible
  198  vim /etc/group
  199  id jenkins
  200  id aws
  201  passwd ansible
  202  passwd ansible
  203  clear
  204  history
  205  clear
  206  userdel aws
  207  ls
  208  ls /home
  209  userdel -r aws
  210  userdel -r jenkins
  211  ls /home
  212  groupdel devops
  213  userdel -r ansible
  214  rm rf /home/aws
  215  ls
  216  ls /home
  217  rm rf /home/aws
  218  rm -rf /home/aws
  219  ls /home/aws
  220  ls /home
  221  history

 225  mkdir /opt/devopsdir
  226  ls -l /opt
  227  clear
  228  ls -l /opt
  229  groupadd devops
  230  useradd ansible
  231  deluser ansible
  232  userdel ansible
  233  cut -d: f1/etc/passwd
  234  cut -d: -f1 /etc/passwd
  235  useradd jenkins
  236  useradd aws
  237  vim /etc/group
  238  id ansible
  239  ls -ld /opt/devopsdir
  240  ls -l /opt/devopsdir
  241  ls -ld /opt/devopsdir
  242  cat etc/passwd
  243  cat /etc/passwd
  244  ls -ld /opt/devopsdir
  245  chown ansible:devops /opt/devopsdir
  246  ls -ld /opt/devopsdir
  247  chmod o-x /opt/devopsdir
  248  chmod o-r /opt/devopsdir
  249  chmod g+r /opt/devopsdir
  250  ls -ld /opt/devopsdir
  251  su - miles
  252  su - aws
  253  ls /etx
  254  ls /etc
  255  cat /etc/group
  256  ls -ld /opt/devopsdir
  257  chmod g-r /opt/devopsdir
  258  su - aws
  259  mkdir /opt/webdata
  260  ls -ld /opt/webdata
  261  chown aws.devops /opt/webdata
  262  chmod 770 /opt/webdata
  263  ls -ld /opt/webdata
  264  sudo - aws
  265  sudo passwd ansible
  266  su - ansible
  267  clear
  268  visudo
  269  ls -l /etc/sudoers
  270  visudo
  271  NOPASSWD: ALL
  272  history
  273  clear
  274  ls -l /etc/sudoers
  275  visudo
  276  clear
  277  history

logout
